# Backlog

Themes and open issues to not forget — things to check again later. Not user stories (see `USER_STORIES.md` for those).

- Smart lookup for unrecognized merchants: when a transaction can't be matched by the seeded/user keyword list, let the user trigger a "search" for that specific transaction to get a suggested category (on top of manual categorization from US-4). Backend undecided — candidates include an LLM-based guess or an external merchant/business lookup API. Revisit once the app is more mature and the common-merchant seed list already in place is proving insufficient.
- Anthropic's `frontend-design` skill is now installed (`.claude/skills/frontend-design/`) and wired into `react-frontend-expert`'s tools, gated to explicit visual-design/polish asks rather than every new screen (it pushes toward one-off bold "signature" designs, which would fight this app's current consistent plain-Tailwind look across screens if used indiscriminately). Still open: actually try it on a real task and judge whether it measurably helps — no comparison has been done yet.
- Check and optimize which LLM each project agent (`java-springboot-expert`, `react-frontend-expert`, `infra-engineer`, `e2e-verifier`, `architecture-reviewer`, `git-commit-pr-workflow`) actually needs versus what it currently uses (none currently set a `model` in their frontmatter, so all inherit whatever model the invoking session is running). Compare output quality against a cheaper/faster model (e.g. Haiku) for the more mechanical, lower-judgment agents — candidates worth testing first are `git-commit-pr-workflow` (largely procedural: commit/push/PR/merge) and `e2e-verifier` (drives a documented API, doesn't design anything) — versus keeping the current model where real architectural/design judgment is load-bearing (`java-springboot-expert`, `react-frontend-expert`, `architecture-reviewer`). Revisit once there's enough real usage across agents to compare cost and quality meaningfully, not just guess.
