# Backlog

Themes and open issues to not forget — things to check again later. Not user stories (see `USER_STORIES.md` for those).

- Smart lookup for unrecognized merchants: when a transaction can't be matched by the seeded/user keyword list, let the user trigger a "search" for that specific transaction to get a suggested category (on top of manual categorization from US-4). Backend undecided — candidates include an LLM-based guess or an external merchant/business lookup API. Revisit once the app is more mature and the common-merchant seed list already in place is proving insufficient.
- Anthropic's `frontend-design` skill is now installed (`.claude/skills/frontend-design/`) and wired into `react-frontend-expert`'s tools, gated to explicit visual-design/polish asks rather than every new screen. Trialed once, as a scoped single-screen redesign of the FE-8 landing page (a "greenbar" bank-statement visual treatment) — the agent's own honest verdict was that it's genuinely more distinctive but now sits as a stylistic outlier next to the other four screens' plain Tailwind look. Still open: decide whether to extend the direction to the rest of the screens, keep it as a one-off, or roll it back.
- LLM-per-agent cost/quality check: both `git-commit-pr-workflow` and `e2e-verifier` were trialed on `claude-haiku-4-5` and kept on it going forward — quality held up on real tasks (a full commit/PR/test/self-review cycle, and a full documented-API sweep). One real gap surfaced and was fixed: `git-commit-pr-workflow` had no rule against stopping a process it didn't start, and killed a running dev server to unblock its own `npm ci`; added an explicit "never stop what you didn't start, report the blocker instead" rule, mirroring `e2e-verifier`'s existing convention. Bonus: the `e2e-verifier` trial also turned up a genuine bug (oversized uploads hung instead of returning the documented 413, root-caused to Tomcat's `max-swallow-size` default — now fixed). `java-springboot-expert`, `react-frontend-expert`, and `architecture-reviewer` remain intentionally on the default model since real architectural/design judgment is load-bearing there.
